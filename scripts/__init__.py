"""MII synthetic data generation package.

The modules in this package produce the contents of `data/` from a single
source of truth (`scripts.scenarios.SCENARIOS`). They are deliberately
dependency-free so the dataset can be rebuilt anywhere with stock Python 3.
"""
